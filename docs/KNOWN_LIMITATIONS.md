# KNOWN_LIMITATIONS

## Open items after Sprint 14

- iOS Safari on physical hardware remains pending.
- Android Chrome on physical hardware remains pending.
- `www.suberos.com` serves the site but does not yet redirect to `https://suberos.com/`.
- Real approved portfolio cases remain pending; Portfolio Lab stays internal and empty by design.
- Mobile LCP still needs validation on physical low-end devices, not only desktop-class local browsers.
- Visual baselines are Chromium-only by design to control cost and noise.
- The production contact endpoint is active and receipts are verified, but a manual mailbox reply test using `Reply-To` is still pending.
